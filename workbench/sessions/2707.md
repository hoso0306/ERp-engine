<!--
File dùng chung để ghi lại các thay đổi trong một phiên làm việc, phục vụ lên
kế hoạch deploy lên VPS production. Quy ước:

- Mỗi việc/tính năng trong phiên ghi thành 1 mục (## 1., ## 2., ...).
- Với thay đổi CODE: nêu file đã sửa/mới, có migration DB không, đã verify gì
  (tsc/eslint/build/test), trạng thái deploy (đã/chưa lên production).
- Với thay đổi DATA (Master Data, sản phẩm, giá...) làm qua script/DB trực
  tiếp trên dev: PHẢI ghi rõ đây là thay đổi dữ liệu chứ không phải code, vì
  deploy sẽ cần tạo lại đúng dữ liệu này trên DB production (không tự sinh
  ra được từ migration).
- Cuối file nên có mục "Deploy note" tóm tắt việc cần làm khi đưa lên production.

Sau khi đã deploy xong lên production, xoá nội dung bên trong (nhưng giữ lại
khối hướng dẫn này) để phiên làm việc tiếp theo có chỗ ghi tiếp.
-->

# Phiên làm việc 27/08/2026 — Return: phân bổ Phí khách/Công ty hỗ trợ, Manual Adjustment, Dashboard, hiển thị đơn hàng

Chủ đề xuyên suốt: cho phép tạo Return ở mọi trạng thái đơn (trừ Huỷ), phân bổ
giá trị hoàn giữa "Phí khách"/"Công ty hỗ trợ", giảm trừ công nợ (Manual
Adjustment) không qua thu tiền thật, và phản ánh đúng ảnh hưởng của Return lên
Dashboard/danh sách Đơn hàng/trang Khách hàng.

---

## 1. Return: nới điều kiện tạo + phân bổ Phí khách/Công ty hỗ trợ

- Cho phép tạo Return ở mọi trạng thái `SalesOrder`, chỉ chặn `CANCELLED`
  (trước đây chỉ cho khi `DELIVERED`).
- Thêm `customerBorneAmount`/`companyBorneReason` trên `Return` — bắt buộc
  nhập lý do khi phần Công ty hỗ trợ > 0.
- Snapshot `ownerId`/`ownerName` (từ `SalesOrder.ownerId`) lên `Return` — phục
  vụ Dashboard "Doanh số theo nhân viên" sau này.

**File:** `apps/api/src/return/return.service.ts`, `return.service.spec.ts`,
`dto/create-return.dto.ts`, `apps/web/src/app/returns/new/page.tsx`.

**Bổ sung (bỏ sót ở bản ghi trước, đã đối chiếu lại với plan 26/08):**
- `apps/web/src/components/sales-order/sales-order-typeahead.tsx` — bỏ
  hard-code lọc `status="DELIVERED"` khi chọn đơn để tạo Return, đổi sang
  `excludeStatus="CANCELLED"`.
- `apps/api/src/sales-order/dto/sales-order-query.dto.ts` — thêm
  `excludeStatus` để hỗ trợ typeahead trên.
- `GET /sales-orders` trả thêm `hasReturn: boolean` (Derived, EXISTS) +
  `apps/web/src/components/sales-order/sales-order-table.tsx` hiện icon/badge
  cảnh báo trên dòng đơn có Return.
- `apps/web/src/app/orders/[id]/page.tsx` — khối **"Đã hoàn"** (chi tiết sản
  phẩm/giá trị hoàn của đơn) trên trang chi tiết đơn hàng (web) — bản in PDF
  xem mục 4.

**Migration:** `20260827064849_return_owner_customer_borne_debt_manual_adjustment`.

**Verify:** `tsc --noEmit`, `jest` pass. Chưa deploy production.

---

## 2. Debt: Manual Adjustment — giảm công nợ không qua thu tiền thật

- API mới `POST /receivables/:id/manual-adjustment` — giảm thẳng
  `Receivable.totalAmount`/`remainingAmount`, KHÔNG tạo `Payment` (không vào
  báo cáo dòng tiền mặt). Ghi `SalesOrderTimeline` action
  `DEBT_MANUAL_ADJUSTED`.
- Trigger tự động ngay sau khi tạo Return có phần Công ty hỗ trợ > 0 (FE gọi
  liền, không hỏi lại) + nút dự phòng thủ công trên trang chi tiết Return, có
  `DebtAdjustmentGate` cảnh báo tránh giảm trùng.
- Permission mới: `debt.manual-adjustment` (đã thêm vào `seed.ts`
  `PERMISSION_CATALOG.debt`).

**File:** `apps/api/src/debt/debt.service.ts`, `debt.service.spec.ts`,
`dto/manual-adjustment.dto.ts` (mới), `receivable.controller.ts`,
`apps/api/prisma/seed.ts`, `apps/web/src/components/return/manual-adjustment-dialog.tsx`
(mới), `apps/web/src/components/return/debt-adjustment-gate.tsx` (mới),
`apps/web/src/app/returns/new/page.tsx`, `apps/web/src/app/returns/[id]/page.tsx`.

**Bổ sung (bỏ sót ở bản ghi trước):** `RECEIVABLE_DETAIL_INCLUDE` (trong
`debt.service.ts`) thêm include `salesOrder.timeline` lọc action
`DEBT_MANUAL_ADJUSTED` — `apps/web/src/app/debts/[id]/page.tsx` hiện khối
**"Lịch sử điều chỉnh công nợ"** riêng, cạnh "Lịch sử thu tiền".

**Migration:** gộp trong `20260827064849_...` (thêm field) +
`20260827080038_return_debt_adjusted_tracking`,
`20260827090517_return_debt_adjusted_by_name` (2 migration này SAU ĐÓ đã bị
thay thế bởi mục 6 — xem "Deploy note").

**DATA (không phải code):** đã thêm permission `debt.manual-adjustment` vào
role ACCOUNTANT/OWNER/ADMIN qua `seed.ts` — trên production cần chạy lại logic
seed permission (hoặc upsert tương đương) để role hiện có được cấp quyền này.

**Verify:** `tsc`, `jest` pass. Chưa deploy production.

---

## 3. SalesOrderItem: snapshot `warnings`/`applicableSurcharges` (fix bản in thiếu phụ phí)

- Phát hiện khi kiểm tra BG000041/SO000034: bản in đơn hàng thiếu cảnh báo phụ
  phí vì `SalesOrderItem` không snapshot 2 field này từ `QuotationItem` lúc
  Approve.
- Thêm `SalesOrderItem.warnings`/`applicableSurcharges` (Json), snapshot trong
  `QuotationWorkflowService.approve()`.

**File:** `apps/api/prisma/schema.prisma`, `apps/api/src/quotation/quotation-workflow.service.ts`.

**Migration:** `20260827071647_sales_order_item_warnings_snapshot`,
`20260827073007_sales_order_item_applicable_surcharges_snapshot`.

**DATA (không phải code):** đã backfill thủ công (script one-off, không qua
seed) cho riêng `SO000034` — các đơn khác approve TRƯỚC khi có 2 migration này
trên production cũng sẽ thiếu dữ liệu này, cần xem xét backfill tương tự nếu
cần in lại đúng.

**Verify:** `tsc`, build pass. Chưa deploy production.

---

## 4. Print page (`quotations/[id]/print`) — hiển thị hàng hoàn trên bản in đơn hàng

- Cột Chú thích: overlay "ĐÃ HOÀN N/M SP" (không thay thế nội dung gốc).
- Cột Thành Tiền: thêm dòng trừ đỏ cho phần Công ty hỗ trợ mỗi dòng hàng hoàn.
- Khối "Đã hoàn" (chi tiết từng phiếu hoàn) đặt ngay sau bảng sản phẩm, TRƯỚC
  khối TỔNG (tách bảng riêng).
- Bỏ hoàn toàn gạch ngang dòng sản phẩm bị hoàn (thử rồi bỏ theo phản hồi).

**File:** `apps/web/src/app/quotations/[id]/print/page.tsx`.

**Verify:** `tsc`, build pass. Không có migration. Chưa deploy production.

---

## 5. Dashboard: trừ phần Công ty hỗ trợ vào doanh thu + khối "Doanh số theo nhân viên"

- `getSalesDashboard()`: `totalRevenue` trừ thêm
  `ReturnService.getTotalCompanyBorneValue(range)`.
- Khối mới "Doanh số theo nhân viên" (`GET /dashboard/sales/by-employee`),
  bộ lọc riêng mặc định "Tháng này".

**File:** `apps/api/src/dashboard/dashboard.service.ts`,
`dashboard.controller.ts`, `dashboard.service.spec.ts`,
`apps/web/src/app/dashboard/page.tsx`,
`apps/web/src/components/dashboard/employee-revenue-panel.tsx` (mới).

**Verify:** `tsc`, `jest` pass. Không có migration riêng. Chưa deploy production.

---

## 6. Bảng `DebtAdjustment` mới — thay cho lưu cộng dồn trên Return

Rà soát lại kiến trúc Manual Adjustment (mục 2): ban đầu thử lưu
`Return.debtAdjustedAt/Amount/ByName` trực tiếp trên Return (kể cả 1 lần sửa
giữa chừng để cộng dồn đúng khi giảm nhiều lần) — sau đó quyết định thay hẳn
bằng 1 bảng riêng, đúng nguyên tắc ưu tiên Source Data hơn Derived Data trùng
lặp.

- Model mới `DebtAdjustment` — nguồn dữ liệu gốc duy nhất cho MỌI khoản giảm
  trừ công nợ (cả gắn Return lẫn độc lập), có `ownerId`/`ownerName` (người phụ
  trách, để tính doanh số nhân viên) tách biệt `createdBy` (người bấm nút).
- **Xoá** `Return.debtAdjustedAt/debtAdjustedAmount/debtAdjustedByName` khỏi
  DB — `ReturnService.findOne()`/`complete()` tự SUM/query lại từ
  `DebtAdjustment`, trả về response cùng tên field như cũ (FE không đổi).
- **Nút "Giảm trừ công nợ" độc lập** (mới) — thêm ở trang chi tiết Receivable
  (`/debts/[id]`), lý do bất kỳ, không cần Return, `ownerId` mặc định lấy từ
  salesperson của đơn hàng liên quan.
- **Tab "Lịch sử giảm trừ/công nợ đầu kỳ"** (mới) — trong Customer Debt Tab,
  gộp `DebtAdjustment` + `OpeningBalanceTimeline` (tăng công nợ đầu kỳ).
- Dashboard "Doanh thu kế hoạch"/"Doanh số theo nhân viên" trừ thêm phần giảm
  trừ độc lập (`returnId = null`, tránh trùng phần Return-driven).

**File:**
- `apps/api/prisma/schema.prisma` (model `DebtAdjustment` mới, xoá 3 field Return)
- `apps/api/src/debt/debt.service.ts`, `debt.service.spec.ts`
- `apps/api/src/debt/debt-adjustment.controller.ts` (mới)
- `apps/api/src/debt/dto/debt-adjustment-history-query.dto.ts` (mới)
- `apps/api/src/debt/debt.module.ts`
- `apps/api/src/return/return.service.ts`, `return.service.spec.ts`
- `apps/api/src/dashboard/dashboard.service.ts`, `dashboard.service.spec.ts`
- `apps/web/src/app/debts/[id]/page.tsx`
- `apps/web/src/components/customer/customer-debt-tab.tsx`
- `apps/web/src/components/customer/debt-adjustment-history-table.tsx` (mới)

**Migration:** `20260827170710_debt_adjustment_table` (tạo bảng),
`20260827170820_return_drop_debt_adjusted_columns` (xoá 3 cột cũ trên `returns`
— **thay thế** 2 migration `return_debt_adjusted_tracking`/
`return_debt_adjusted_by_name` ở mục 2, 2 cột đó không còn tồn tại sau migration
này).

**DATA (không phải code):** trước khi xoá 3 cột cũ trên `returns`, đã chạy 1
script one-off backfill 5 bản ghi lịch sử TEST trên DB dev (từ
`SalesOrderTimeline` action `DEBT_MANUAL_ADJUSTED` sang bảng `DebtAdjustment`
mới) — thuần để không mất dữ liệu test đã tạo lúc rà soát tính năng ở mục 1-2.

**Không cần backfill trên production:** toàn bộ tính năng Manual Adjustment
(mục 2) lẫn bảng `DebtAdjustment` (mục 6) đều là **xây mới hoàn toàn trong
phiên làm việc này** — production chưa từng chạy 2 tính năng này trước đó, nên
chắc chắn `sales_order_timelines` trên production không có bản ghi action
`DEBT_MANUAL_ADJUSTED` nào (bảng `returns` production cũng chưa từng có 3 cột
`debt_adjusted_*` để mà cần xoá theo migration mục 2/6). File script backfill
(đã xoá sau khi dùng xong trên dev) vì vậy **không cần viết lại** — chỉ là
công cụ dùng riêng cho dữ liệu test lúc phát triển.

**Verify:** `tsc --noEmit` sạch cả 2 app, `nest build`/`next build` thành
công, `jest` 370/387 tại thời điểm này (17 fail là lỗi có sẵn từ trước — thiếu
`PermissionService` provider trong `sales-order.service.spec.ts`, xác nhận qua
`git stash`, không liên quan phiên này; **đã fix ở mục 10**, baseline cuối
phiên là 387/387). Đã xác nhận route sống trên dev server (401 = tồn tại,
cần auth). Chưa deploy production.

**Ghi chú thiếu sót:** kế hoạch ban đầu (`2026-08-27-debt-adjustment-unify.md`)
có liệt kê cập nhật `knowledge/modules/customer.md`, nhưng thực tế **chưa cập
nhật** — lúc làm không thấy nội dung phù hợp để thêm vào cấu trúc file đó.
Không ảnh hưởng deploy (file `.md` trong `knowledge/` không lên production),
chỉ ghi nhận lại cho đầy đủ.

---

## 7. Hiển thị "Tổng tiền" đơn hàng đã trừ hàng hoàn

- `GET /sales-orders` trả thêm `netAmount = totalAmount − SUM(phần Công ty hỗ
  trợ của các Return thuộc đơn)` — chỉ để hiển thị, KHÔNG đổi `totalAmount`
  gốc (giữ nguyên "Immutable Document").
- Cột "Tổng tiền" ở danh sách Đơn hàng + tab "Đơn hàng" của Khách hàng dùng
  `netAmount` thay `totalAmount`.
- Trang chi tiết đơn hàng: thêm 2 dòng màu đỏ nhạt dưới "Tổng thanh toán" —
  "Tổng giảm trừ hàng hoàn" và "Tổng giá trị đơn hàng" — chỉ hiện khi đơn có
  hàng hoàn.

**File:** `apps/api/src/sales-order/sales-order.service.ts`,
`apps/web/src/components/sales-order/sales-order-table.tsx`,
`apps/web/src/components/sales-order/sales-order-item-table.tsx`,
`apps/web/src/app/orders/[id]/page.tsx`, `apps/web/src/app/orders/page.tsx`.

**Verify:** `tsc`, `nest build`/`next build`, `jest` (cùng baseline mục 6).
Không có migration riêng (chỉ đổi logic đọc). Chưa deploy production.

---

## 8. "Tổng doanh số" khách hàng — endpoint aggregate riêng (không giới hạn theo trang)

- Endpoint mới `GET /sales-orders/revenue-summary?customerId=&from=&to=` —
  aggregate đúng TOÀN BỘ khoảng lọc ở DB (khác bảng đơn hàng bên cạnh, vẫn lọc
  ngày phía FE trên dữ liệu 1 trang — giới hạn có sẵn, ngoài phạm vi sửa lần
  này). Loại đơn `CANCELLED`. Trừ đúng phần Công ty hỗ trợ của Return thuộc
  các đơn TẠO trong kỳ lọc (khớp đúng ý nghĩa `netAmount` ở mục 7).
- `SalesOrderModule` import thêm `ReturnModule` để Controller ghép kết quả 2
  service (không service nào đọc thẳng bảng của module kia).
- Tab "Đơn hàng" của Khách hàng: "Tổng doanh số" đổi từ tính tay ở FE (chỉ
  đúng trong trang đang xem) sang gọi endpoint mới.

**File:** `apps/api/src/sales-order/sales-order.service.ts`,
`sales-order.controller.ts`, `sales-order.module.ts`,
`apps/api/src/return/return.service.ts`,
`apps/web/src/components/customer/customer-orders-tab.tsx`.

**Verify:** `tsc`, `nest build`/`next build`, `jest` (cùng baseline mục 6).
Không có migration. Đã xác nhận route sống trên dev server (401). Chưa deploy
production.

---

## 9. Rà soát dữ liệu — RT000001/RT000002 (không thay đổi gì, chỉ ghi nhận quyết định)

Phát hiện khi kiểm tra dữ liệu dev: 2 phiếu hoàn có sai lệch giảm trừ công nợ:

- **RT000001** (SO000018, khách "Nguyễn Văn Demo"): cần giảm 649.440đ nhưng
  `Receivable.remainingAmount` chỉ còn 59.040đ (khách đã trả gần hết) — không
  thể giảm đủ, thiếu cả lý do (`companyBorneReason` rỗng, tạo trước khi có
  validate bắt buộc).
- **RT000002** (SO000034, khách "test hoan huy" — dữ liệu test): đã bị giảm
  trừ VƯỢT quá phần Công ty hỗ trợ thực tế 2.782.000đ (do bấm test nhiều lần
  trước đó), không phải "chưa giảm trừ".

**Quyết định của người dùng: để nguyên hiện trạng cả 2, không xoá, không tạo
thêm giảm trừ, không sửa gì.** Đây KHÔNG phải thay đổi cần deploy — chỉ ghi
nhận lại để nếu sau này cần xử lý (refund tiền mặt đã thu cho RT000001, hoặc
xây khả năng "tăng lại công nợ" để sửa RT000002) thì biết bối cảnh.

---

## 10. Fix 17 test fail có sẵn ở `sales-order.service.spec.ts` (không liên quan nghiệp vụ Return)

Trước đó vẫn báo "370/387, 17 fail có sẵn từ trước, không liên quan phiên
này" ở các mục 6-8 — nay đã fix luôn, baseline sạch 100%.

- **Nguyên nhân:** `SalesOrderService` có `PermissionService` là tham số thứ 3
  trong constructor (dùng cho check quyền `export-all` ở
  `getRevenueByCustomer`), nhưng 3 chỗ dựng `TestingModule` trong
  `sales-order.service.spec.ts` (khối `ship/deliver/override/cancel/...`,
  `getTodaySummary()`, `getDashboardSummary()`) chỉ mock `PrismaService`/
  `SettingService`, thiếu hẳn `PermissionService` — NestJS không dựng nổi
  service, toàn bộ 17 test trong file văng lỗi ngay từ `beforeEach`. Lỗi có
  từ trước phiên này (đã xác nhận qua `git stash` lúc đầu phiên), không phải
  do các thay đổi ở mục 1-9 gây ra.
- **Fix:** thêm `{ provide: PermissionService, useValue: { hasPermission: jest.fn() } }`
  vào cả 3 nơi — đúng pattern đã dùng ở `quotation-workflow.service.spec.ts`.
  Không cần mock chi tiết hơn vì các test trong file không đụng nhánh gọi
  `hasPermission()`.

**File:** `apps/api/src/sales-order/sales-order.service.spec.ts` (chỉ thêm
import + 3 dòng provider, không đổi logic test nào).

**Verify:** `tsc --noEmit` sạch, `jest` **387/387 pass** (trước đó 370/387).
Không có migration, không ảnh hưởng runtime code (chỉ sửa file test).

---

## 11. Trang mới "Tài khoản của tôi" (mục riêng ở sidebar) — số liệu cá nhân từng nhân viên

Trang cá nhân cho MỌI người dùng đã đăng nhập (không phân biệt role) — tên/
email/phone/lời chào (đọc thẳng từ `useAuth()`, không cần API mới), doanh số
+ số đơn của chính mình (có bộ lọc ngày, bấm vào mở thẳng danh sách đơn hàng
tương ứng của mình, loại đơn Đã huỷ), tổng công nợ đang quản lý (số hiện tại),
và biểu đồ cột so sánh doanh số các nhân viên đã phát sinh doanh thu trong kỳ.

- **Endpoint mới** `GET /dashboard/me/summary?from=&to=` — **cố ý không gắn
  `@RequirePermission`**, chỉ trả số của đúng `req.user.userId` từ JWT (không
  nhận `ownerId` từ query) nên không cần `sales-order.view-cost`/`debt.view`
  như các khối xem số NGƯỜI KHÁC. Doanh số tái dùng đúng
  `getEmployeeRevenueDashboard()` có sẵn (đã trừ Công ty hỗ trợ + giảm trừ độc
  lập), chỉ lọc lấy đúng 1 dòng.
- **`DebtService.getTotalRemainingByOwner(ownerId)`** (method mới) —
  `SUM(Receivable.remainingAmount)` theo `ownerId`, loại `CANCELLED`.
- **Biểu đồ cột so sánh nhân viên** (component mới `employee-revenue-bar-chart.tsx`,
  dùng `recharts` đã có sẵn trong dự án) — vẫn gọi `GET /dashboard/sales/by-employee`
  đã có sẵn, giữ nguyên gate `sales-order.view-cost` (số của người khác).
- **`orders/page.tsx`** — bổ sung đọc filter ban đầu từ URL (`useSearchParams`,
  trước đây chỉ quản lý bằng React state thuần) để hỗ trợ deep-link từ trang
  cá nhân: `/orders?ownerId=self&createdFrom=&createdTo=&excludeStatus=CANCELLED`.
- **Đổi tên tránh trùng:** đặt tên **"Tài khoản của tôi"** (không phải
  "Người dùng" như đề bài gốc — tên đó đã bị trang quản lý user của admin
  chiếm, đã hỏi và chốt lại với người dùng).
- **Vị trí (sửa 28/08/2026):** ban đầu làm thành 1 tab lồng trong trang
  Settings (`settings-tabs.tsx`) — sau đó theo yêu cầu, tách ra thành **mục
  riêng ở sidebar**, đặt ngay dưới "Cài đặt" (nhóm "Hệ thống",
  `config/navigation.ts`), **không gắn `requiredPermission`** (khác "Cài đặt"
  vốn cần `settings.view`) để mọi role (kể cả SALES không có `settings.view`,
  không thấy được "Cài đặt") vẫn thấy trang cá nhân của chính họ. Kèm theo:
  sửa `app-sidebar.tsx` để tính "mục đang active" theo href DÀI NHẤT khớp
  trong toàn bộ menu (không phải so khớp độc lập từng mục) — cần thiết vì
  `/settings` và `/settings/me` giờ là 2 mục ngang hàng nhưng href của
  `/settings/me` vẫn có tiền tố `/settings`, nếu không sửa sẽ sáng đèn nhầm cả
  2 mục cùng lúc khi đang ở trang cá nhân.

**File:**
- `apps/api/src/dashboard/dashboard.controller.ts`, `dashboard.service.ts`, `dashboard.service.spec.ts`
- `apps/api/src/debt/debt.service.ts`, `debt.service.spec.ts`
- `apps/web/src/app/settings/me/page.tsx` (mới)
- `apps/web/src/config/navigation.ts`
- `apps/web/src/components/layout/app-sidebar.tsx`
- `apps/web/src/components/setting/settings-tabs.tsx` (thêm rồi bỏ lại — mục
  "me" không còn nằm trong mảng `SETTINGS_TABS`)
- `apps/web/src/components/dashboard/employee-revenue-bar-chart.tsx` (mới)
- `apps/web/src/app/orders/page.tsx`

**Thêm cho vui (28/08/2026, thuần FE, không đổi backend):**
- **Huy hiệu xếp hạng** ("#2/8") — suy ra từ chính `employeeRows` đã fetch cho
  biểu đồ so sánh (đã sort sẵn ở BE), không gọi thêm API. Chỉ hiện khi có
  quyền `sales-order.view-cost` (cần thấy số người khác mới xếp hạng được).
- **"So với kỳ trước"** (mũi tên +/-%) — gọi lại đúng `GET /dashboard/me/summary`
  với khoảng ngày liền trước, CÙNG ĐỘ DÀI khoảng đang chọn (không cố định
  "tháng trước" theo lịch). Không cần permission gì thêm (vẫn là số của
  chính mình).

**Verify:** `tsc --noEmit` sạch cả 2 app, `nest build`/`next build` thành
công, `jest` **391/391 pass** (thêm 4 test mới cho `getTotalRemainingByOwner()`/
`getMySummary()`). Không có migration (không đổi schema). Đã xác nhận route
`/dashboard/me/summary` sống trên dev server (401 = tồn tại, cần auth) và gọi
thử trực tiếp qua script cho user "Admin" ra số hợp lý (doanh số 69.493.650đ/16
đơn, công nợ 48.794.550đ trong tháng 8/2026 — dữ liệu dev thật). Chưa deploy
production. Chưa test UI trực tiếp trên trình duyệt (không có công cụ chụp
màn hình trong phiên này).

---

# Deploy note

1. **Chạy migration theo đúng thứ tự** (thư mục `apps/api/prisma/migrations`,
   từ `20260827064849_...` đến `20260827170820_return_drop_debt_adjusted_columns`).
   Lưu ý mục 6: 2 migration `return_debt_adjusted_tracking`/
   `return_debt_adjusted_by_name` (mục 2) và `debt_adjustment_table`/
   `return_drop_debt_adjusted_columns` (mục 6) phải chạy **theo đúng thứ tự
   thời gian tạo** — không bỏ qua migration nào dù cột bị xoá ngay sau đó,
   Prisma cần đủ lịch sử để đồng bộ `_prisma_migrations`.
2. **Không cần backfill DebtAdjustment** (mục 6) — toàn bộ tính năng Manual
   Adjustment/`DebtAdjustment` mới xây trong phiên này, production chưa từng
   chạy nên chắc chắn chưa có bản ghi `DEBT_MANUAL_ADJUSTED` nào. Có thể chạy
   câu sau để xác nhận cho chắc trước khi migrate (kỳ vọng kết quả = 0):
   ```sql
   SELECT COUNT(*) FROM sales_order_timelines WHERE action = 'DEBT_MANUAL_ADJUSTED';
   ```
3. **Seed lại permission `debt.manual-adjustment`** (mục 2) cho các role hiện
   có trên production (ACCOUNTANT/OWNER/ADMIN) — chạy `seed.ts` (nếu an toàn
   re-run) hoặc 1 script upsert permission riêng, KHÔNG re-run toàn bộ seed
   nếu seed không idempotent với dữ liệu người dùng thật.
4. **Cân nhắc backfill `SalesOrderItem.warnings`/`applicableSurcharges`**
   (mục 3) cho các đơn hàng đã Approve trước khi lên production, nếu cần bản
   in hiển thị đúng cảnh báo/phụ phí cho các đơn cũ.
5. Không có thay đổi Tech Stack, không có breaking change ở API cũ (chỉ thêm
   field/endpoint mới, hoặc đổi field không lưu DB sang tính runtime).
