{
  "name": "erp-engine",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "build:netlify": "pnpm --filter ./apps/web run build:netlify",
    "lint": "pnpm -r lint"
  }
}
